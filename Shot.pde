class Shot{
  int maxShotCount = 20;
  int shotcount;
  
  
  void keyReleased() {
      
      //if (shotcount == 0) return;
      
      //if (gazeTrack.gazePresent()) { 
      //    myMask.shoted(gazeTrack.getGazeX(), gazeTrack.getGazeY()); 
      //    myGaze.shoted(server);
      //} 
      
      //shotcount = shotcount - 1;
      //println("shootcount: " + shotcount);
  }
}
